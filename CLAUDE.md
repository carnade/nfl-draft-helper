When writing patch notes, always write about the functionality but never the technical.

When updating the changelog in README.md, also update `MANUAL_LAST_UPDATED` in `src/LeftMenu.js` to today's date (ISO format `YYYY-MM-DDT00:00:00.000Z`). This controls whether the changelog indicator shows as unread for users.
